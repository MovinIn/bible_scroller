# Session log — 2026-07-16 21:27

**Repo:** `c:\Users\Joseph\Documents\workspaces\flutter\bible_scroller` (no `.git` at workspace root)
**Branch:** n/a
**Trigger:** /close

## Summary

Implemented real accounts for Bible Scroller: FastAPI JWT auth with Google Sign-In and email/password, 6-digit email verification, public browse with login-gated likes/comments. Followed with a code-review remediation pass covering account takeover paths, session restore, SMTP/JWT hardening, rate limits, and client UX/tests.

## Accomplished

- Server: `User` auth fields + Alembic `003_auth_accounts`; `/auth/register|login|verify-email|resend-verification|google`; `require_user` / `get_optional_user`; bcrypt; hashed 6-digit codes; Capture/Console/Smtp mailers; in-memory rate limiter.
- Client: `AuthService`/`AuthController`/`AuthSheet`/`auth_gate`; Bearer + device-link header; Google Sign-In adapter (v7 API); secure token storage; gated like/comment actions.
- Review fixes: squat overwrite; Google clears unverified password; Google `email_verified` claim; device_id collision safe; restore via `GET /users/me`; production JWT/SMTP fail-fast; 429 rate limits; user-facing errors; cancel Google silently.
- Tests: server suite ~64 passed; Flutter auth-related tests ~17 passed (plus full client suite earlier).

## Decisions

- **No Firebase:** verify Google ID tokens on FastAPI; issue app JWTs.
- **Browse freely:** reads public; writes need verified login.
- **6-digit codes in-app** (not magic links).
- **Anti-enumeration:** verified re-register returns same `verification_required` shape.
- **Verified password + Google:** allow link, keep password; unverified squat loses password on Google.

## Open tasks

- [ ] Commit auth work (priority: high) — workspace root had no git; locate real repo / init if needed
- [ ] Configure production env: `JWT_SECRET`, `GOOGLE_CLIENT_IDS`, `SMTP_*`, `ENVIRONMENT=production`, `ALLOW_CONSOLE_MAILER=false` (priority: high)
- [ ] Platform Google OAuth setup (Android SHA-1 / iOS URL scheme) (priority: high)
- [ ] Run `alembic upgrade head` on deployed DB (priority: high)
- [ ] Apple Sign-In / password reset (priority: low — deferred)

## Blockers

- none for local implementation; production Google OAuth client IDs and SMTP not configured in-session

## Files touched (high level)

- `scroller/server/src/services/auth.py`, `mailer.py`, `rate_limit.py`, `deps.py`, `config.py`, `routes/auth.py`, `models.py`, `main.py`
- `scroller/server/alembic/versions/003_auth_accounts.py`, `tests/test_auth.py`, `conftest.py`
- `scroller/client/lib/services/auth_service.dart`, `state/auth_controller.dart`, `widgets/auth_sheet.dart`, `auth_gate.dart`, `main.dart`, `api_client.dart`
- `scroller/client/test/state|widgets|services/*auth*`
- `scroller/README.md`

## Git state

- **Before close:** workspace root is not a git repository (`fatal: not a git repository`)
- **Commits this session:** none
- **Handoff commit:** pending — say **build** / `/close and commit` if you want a commit plan once git root is identified

## Memory updates

- `.cursor/MEMORY.md`: created (standing decisions + active auth context)
- `~/.cursor/MEMORY.md`: skipped

## Next session

1. Read `.cursor/MEMORY.md` and this log.
2. Find or init git remote for `scroller` (or whole monorepo) and commit auth changes.
3. Wire Google Cloud OAuth + production SMTP/JWT env; migrate DB.
